[{% for distribution in results -%}
    {{ {id: distribution.id, label: distribution.label, value: distribution.value}|json_encode|raw }}
    {# use "value" instead of "id" key, if you use jquery-ui #}
    {%- if not loop.last %},{% endif -%}
{%- endfor %}]